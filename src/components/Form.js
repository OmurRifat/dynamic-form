import React, { useState } from 'react';

const Form = () => {
    const [sectors, setSectors] = useState([]);
    const [selected, setSelected] = useState("")
    return (
        <div>
            <h4>This is from Form.js file, Hope you will enjoy</h4>
        </div>
    );
};

export default Form;